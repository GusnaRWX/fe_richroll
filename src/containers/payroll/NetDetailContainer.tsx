import React from 'react';
import Layout from '@/components/_shared/_core/layout/Index';
import NetDetail from '@/components/payroll/generate/NetDetail';

const NetDetailContainer = () => {
  return (
    <Layout>
      <NetDetail />
    </Layout>);
};

export default NetDetailContainer;